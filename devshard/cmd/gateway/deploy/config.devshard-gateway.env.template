export GATEWAY_CHAIN_ID=gonka-mainnet
export GATEWAY_CHAIN_RPC=http://node:26657
export GATEWAY_CHAIN_GRPC=node:9090
export GATEWAY_PUBLIC_API=http://api:9000

export GATEWAY_INSTANCE_NAME=devshard-gateway
export GATEWAY_PORT=8080
export GATEWAY_HOST_PORT=18080
export GATEWAY_STORAGE_DIR=/root/.devshard-gateway
export GATEWAY_STORAGE_HOST_DIR=.devshard-gateway

export GATEWAY_PRIVATE_KEY=**
export GATEWAY_API_KEYS=sk-**
export GATEWAY_ADMIN_API_KEY=sk-admin-**

export GATEWAY_POC_MODE=relaxed
export GATEWAY_MAX_CONCURRENT_REQUESTS=2048
export GATEWAY_ADMISSION_QUEUE_WAIT_MS=300000
export GATEWAY_ADMISSION_QUEUE_PER_SLOT=4
export GATEWAY_MATCH_WAIT_MS=2000
export GATEWAY_MAX_CONSECUTIVE_BURNS=2
export GATEWAY_DEFAULT_MAX_TOKENS=4096
export GATEWAY_MAX_TOKENS_CAP=4096
export GATEWAY_MAX_BUFFERED_RESPONSE_BYTES=536870912
export GATEWAY_ESCROWS_JSON=''

export GATEWAY_TX_GAS_LIMIT=700000

export GATEWAY_ACCOUNTING_ENABLED=false
export GATEWAY_ACCOUNTING_HOST_PORT=19091
export GATEWAY_ACCOUNTING_PORT=9091
export GATEWAY_ACCOUNTING_RETENTION_EPOCHS=2
export GATEWAY_ACCOUNTING_SNAPSHOT_SECONDS=300

# json (the default when empty) or text; the Loki panels read JSON.
export GATEWAY_LOG_FORMAT=json

export GATEWAY_ROTATION_ENABLED=false
export GATEWAY_ROTATION_SETTLEMENT_ENABLED=false
export GATEWAY_ROTATION_MODELS_JSON=''

# How stale the chain snapshot may be before requests are refused; 0 disables the gate.
export GATEWAY_CHAIN_SNAPSHOT_MAX_AGE_SECONDS=60

# Engine timings. All five also take an admin override, so a change needs no restart.
export GATEWAY_ENGINE_RECEIPT_TIMEOUT_MS=5000
export GATEWAY_ENGINE_FIRST_TOKEN_FLOOR_MS=6000
export GATEWAY_ENGINE_FIRST_TOKEN_CEILING_MS=30000
export GATEWAY_ENGINE_INTER_CHUNK_STALL_MS=30000
export GATEWAY_ENGINE_LOSER_GRACE_MS=600000
